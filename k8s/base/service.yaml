apiVersion: v1
kind: Service
metadata:
  name: pae-backend-ot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pae-backend-ot
    # Target only app pods, not the redis/migrate pods that share the name label.
    app: pae-backend-ot-api
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
